![Screenshot](/img/parallax-thumb.jpg)
# parallax-background-example
An example of parallax scrolling. The background (image) moves slower than the foreground (text) by using `background-attachment: fixed`.
